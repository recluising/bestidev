NOTA: MAIL DE PRUEBA. CONTENIDO SIN VALIDEZ.

Estimado Cliente:

Le confirmamos el recibo de su pedido en bestiaz.com. Compruebe que los siguientes datos sobre su pedido son correctos.

Dirección de envío: <%= @order.ship_address.to_s+", "+@order.ship_address.city.to_s+" "+@order.ship_address.zipcode.to_s%>
Dirección de facturación: <%= @order.bill_address.to_s+", "+@order.bill_address.city.to_s+" "+@order.bill_address.zipcode.to_s%>

===========================================================.
Resumen del pedido:
============================================================
<% for item in @order.line_items %>
<%=item.variant.sku %> <%=item.variant.product.name%> <%= variant_options(item.variant) %> (<%=item.quantity%>) @ <%= number_to_currency item.price %> = <%= number_to_currency(item.price * item.quantity) %>
<% end %>
============================================================
Subtotal: <%= number_to_currency @order.item_total %>
<% @order.adjustments.each do |adjustment| %>
<%= "#{adjustment.label}: #{number_to_currency adjustment.amount}"%>
<% end %>
 Total del pedido: <%= number_to_currency @order.total %>

<% if @order.payment_method.name == "Transferencia Bancaria" %>
  Método de pago: Transferencia Bancaria 
  Por favor incluya como motivo de la transferencia "Pedido  <%= @order.number %> " 
  <%= @order.payment_method.description.to_s %>
<% end %>


Gracias por su compra. Si tiene alguna duda, puede contactar con nosotros en info.bestiaz@gmail.com

Saludos,
Tu equipo de confianza www.bestiaz.com
